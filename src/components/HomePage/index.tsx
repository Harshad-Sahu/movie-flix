import React from "react";
import axios from "axios";
import { Header } from "..";
import { API_KEY, GENRE_API_ENDPOINT } from "@/common";

export const HomePage = () => {
  return (
    <>
      <Header />
    </>
  );
};
